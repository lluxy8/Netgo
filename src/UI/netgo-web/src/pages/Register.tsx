import type { PreRegister } from "@/types/dtos";
import { useLoaderData } from "react-router-dom";


export function Register() {
    const data = useLoaderData() as PreRegister;

    return(
      <>
        
      </>
    )
}